# REQUIRED VARIABLES

variable "private_markdown_storage_enforced" {
  description = "(REQUIRED) Is the private tile markdown storage which used to display custom dynamic and static content enabled?"
  type        = bool

}
